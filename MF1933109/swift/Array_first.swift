class Array_first{
/**类名_方法名(_形参类型)*
 * @author: mf1933109
 *  @className: Array_first
 *  @apiSignature: Array$func write(_ buffer: UnsafePointer<UInt8>, maxLength len: Int) -> Int
 *  @description: Test swift api OutputStream$Array$var last: Element? { get }
 *  @Map: LinkedList$public E peekLast()
 */
  static func first0(){
  /**
     * input: 0
     * class0 Array
     */
    let class0 = [10, 20, 30, 40, 50]
    let firstNumber = class0.first
    assert(firstNumber==10)
  }
}
Array_first.first0()